Generate the four Aksa Digital Login preview states from `../docs/login-redesign-prompt.md` and `../docs/design.md`.

Publish them using the Aksa HTML Canvas MCP server:
- idle
- filled
- positive
- negative

Do not modify React or any project source outside this workspace.
