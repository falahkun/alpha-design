# Agent Workspace

This directory is the intended working directory for Claude Code, Codex and Antigravity.

Generated HTML belongs in `frames/` only.

The React application is intentionally outside this workspace boundary.
